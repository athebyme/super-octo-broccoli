# Улучшения функционала объединения карточек

## ✅ Что добавлено

Реализованы три крупных улучшения функционала объединения карточек WB:

---

## 1. 📄 Пагинация товаров

### Зачем нужно
При наличии тысяч карточек страница загружалась очень медленно и была неудобна для работы.

### Что сделано
- **Пагинация по группам**: 50 групп карточек на страницу (настраивается)
- **Навигация**: Первая, Последняя, Назад, Вперед + номера страниц
- **Статистика**: "Показано групп 1-50 из 245"
- **Сохранение фильтров**: При переходе между страницами фильтры сохраняются

### Как использовать
Просто открывайте страницу "Объединение" - пагинация работает автоматически!

---

## 2. 🔍 Расширенные фильтры

### Зачем нужно
Быстро находить нужные карточки среди тысяч товаров.

### Что сделано

#### Три типа фильтров:

1. **Текстовый поиск** (по названию, артикулу, бренду)
   ```
   Например: "Парик длинный черный"
   Найдет все карточки где в названии, артикуле или бренде есть эти слова
   ```

2. **Фильтр по категории** (dropdown)
   ```
   Выберите категорию из списка
   Показывает только товары этой категории
   ```

3. **Фильтр по бренду** (dropdown)
   ```
   Выберите бренд из списка
   Показывает только товары этого бренда
   ```

#### Комбинированные фильтры:
Можно использовать все три фильтра одновременно!

Пример:
- Категория: "Парики"
- Бренд: "BeautyHair"
- Поиск: "длинный"
→ Покажет только длинные парики бренда BeautyHair

### Статистика результатов
После применения фильтров видно:
- Сколько групп найдено
- Сколько всего карточек
- На какой странице находитесь

### Как использовать

1. Откройте **"Объединение"**
2. В верхней панели "Фильтры и поиск":
   - Введите текст в поле поиска
   - Выберите категорию (опционально)
   - Выберите бренд (опционально)
3. Нажмите **"Применить фильтры"**
4. Для сброса: **"Сбросить"**

---

## 3. 🧠 Умные рекомендации для объединения

### Зачем нужно
Автоматически находить карточки, которые стоит объединить, на основе умного алгоритма анализа.

### Как работает алгоритм

Алгоритм анализирует все ваши карточки и ищет похожие товары по критериям:

#### Критерии анализа (с весами):

1. **Бренд (критично, 20%)** - Обязательно одинаковый
   - Разные бренды = не рекомендуется

2. **Название (очень важно, до 40%)**
   - Нормализует названия (убирает размеры, цвета в скобках)
   - Сравнивает базовое название
   - Пример: "Парик длинный (черный, M)" и "Парик длинный (белый, S)" → 95% схожести

3. **Артикулы (важно, до 30%)**
   - Извлекает базовую часть артикула
   - Пример: "ABC-123-RED" и "ABC-123-BLUE" → одинаковый базовый артикул
   - Высокая схожесть даже при разных вариациях

4. **Категория (обязательно, 10%)**
   - Только карточки одной категории (subject_id)
   - Разные категории = не рекомендуется

#### Оценка схожести (Score)

Алгоритм выдает оценку от 0% до 100%:

- **90-100%** 🟢 - Почти идентичные товары, очень высокая уверенность
- **70-89%** 🔵 - Высокая схожесть, рекомендуется объединить
- **60-69%** 🟡 - Средняя схожесть, стоит проверить
- **50-59%** 🟠 - Низкая схожесть, требует внимания
- **< 50%** ⚪ - Не рекомендуется (не показывается по умолчанию)

### Что сделано

#### Новая страница: "Умные рекомендации"

Доступна по кнопке **"Умные рекомендации"** на странице объединения.

#### Возможности:

1. **Топ-50 рекомендаций** - Лучшие находки алгоритма
2. **Настройка порога схожести**:
   - 50% - Показать все
   - 60% - Рекомендуемый (по умолчанию)
   - 70% - Высокая уверенность
   - 80% - Очень высокая
   - 90% - Почти идентичные

3. **Количество результатов**: 10, 20 или 50 лучших рекомендаций

4. **Красивый UI**:
   - Цветовая индикация уверенности (зеленый/синий/желтый)
   - Оценка схожести крупным шрифтом
   - Описание причины рекомендации
   - Рекомендуемая главная карточка отмечена
   - Кнопка "Перейти к объединению" с автоприменением фильтров

#### Статистика:
- Сколько рекомендаций найдено всего
- Сколько карточек можно объединить
- Текущий минимальный порог

### Как использовать

#### Шаг 1: Открыть рекомендации
1. Перейдите в **"Объединение"**
2. Нажмите **"Умные рекомендации"** (фиолетовая кнопка с иконкой лампочки)

#### Шаг 2: Настроить параметры (опционально)
- Минимальная оценка схожести: Выберите порог (рекомендуется 60%)
- Показать топ: Выберите количество (10, 20 или 50)
- Нажмите **"Применить"**

#### Шаг 3: Просмотреть рекомендации
Каждая рекомендация показывает:
- **Оценку схожести** (%) с цветовой индикацией
- **Причину** рекомендации (бренд, похожие названия, артикулы)
- **Список карточек** для объединения
- **Рекомендуемую главную карточку** (первая в списке)

#### Шаг 4: Перейти к объединению
Нажмите **"Перейти к объединению"** на понравившейся рекомендации.

Вы будете перенаправлены на страницу объединения с:
- Автоматически примененными фильтрами (категория + бренд)
- Отфильтрованным списком карточек
- Готовностью к выбору и объединению

---

## 🚀 Инструкция по обновлению

### На сервере выполните:

```bash
# 1. Забрать изменения
git pull origin claude/init-seller-platform-ajnYP

# 2. Перезапустить контейнер (данные сохранятся!)
docker-compose restart seller-platform

# 3. Проверить что всё работает
docker-compose logs seller-platform | tail -50
```

### Альтернатива (с пересборкой):

```bash
# Пересобрать и запустить
docker-compose up -d --build seller-platform
```

---

## 📖 Примеры использования

### Пример 1: Поиск всех париков определенного цвета

1. Откройте **"Объединение"**
2. Фильтры:
   - Категория: "Парики"
   - Поиск: "черный"
3. **"Применить фильтры"**
4. Получите все черные парики

### Пример 2: Объединение вариаций одного товара

1. **"Умные рекомендации"**
2. Найдите рекомендацию с оценкой > 85%
3. Проверьте список карточек (например, разные размеры одного парика)
4. **"Перейти к объединению"**
5. Выберите главную карточку (radio button)
6. Отметьте остальные (checkbox)
7. **"Объединить карточки"**

### Пример 3: Быстрый поиск по артикулу

1. **"Объединение"**
2. Поиск: "ABC-123"
3. **"Применить фильтры"**
4. Получите все товары с артикулом ABC-123-*

---

## 🎯 Рекомендации

### Для лучших результатов рекомендаций:

1. **Соблюдайте единообразие названий**
   - Хорошо: "Парик длинный волнистый (черный, M)"
   - Плохо: "Парик M черн.", "ПаРиК ЧЕРНЫЙ M!!"

2. **Используйте стандартизированные артикулы**
   - Хорошо: "WIG-001-BLK", "WIG-001-WHT"
   - Плохо: "wig1black", "wig_1_white"

3. **Указывайте правильные бренды**
   - Одинаковый бренд = ключевой фактор для рекомендаций

4. **Регулярно проверяйте рекомендации**
   - Алгоритм улучшается с ростом количества карточек
   - После синхронизации новых товаров проверьте рекомендации

### Для эффективного использования фильтров:

1. **Комбинируйте фильтры**
   - Категория + Поиск = быстрее найдете нужное
   - Бренд + Поиск = точнее результаты

2. **Используйте частичный поиск**
   - "длин" найдет "длинный", "длинные", "удлиненный"
   - Поиск без учета регистра

3. **Сбрасывайте фильтры**
   - После работы нажмите "Сбросить" для новых поисков

---

## 🐛 Известные ограничения

1. **Алгоритм рекомендаций**:
   - Анализирует только необъединенные карточки (без imt_id)
   - Максимум 50 топ-рекомендаций
   - Не учитывает историю объединений пользователя

2. **Поиск**:
   - Работает по полям: title, vendor_code, brand
   - Не ищет по nmID (используйте прямой фильтр)

3. **Пагинация**:
   - Группы пагинируются, а не отдельные карточки
   - Группа может содержать разное количество карточек

---

## 📝 Технические детали

### Новые файлы:
- `merge_recommendations.py` - Алгоритм рекомендаций
- `templates/products_merge_recommendations.html` - UI рекомендаций

### Измененные файлы:
- `routes_merge_cards.py` - Добавлены пагинация, фильтры, роут рекомендаций
- `templates/products_merge.html` - Обновлен UI фильтров, добавлена пагинация

### Новый роут:
- `GET /products/merge/recommendations` - Страница умных рекомендаций

### Зависимости:
- SQLAlchemy `or_()` - Для комбинированного поиска
- Python `difflib.SequenceMatcher` - Для вычисления схожести строк

---

## 🎉 Итого

Все три улучшения работают вместе для максимального удобства:

1. **Пагинация** - Быстрая загрузка даже при тысячах товаров
2. **Фильтры** - Точный поиск нужных карточек
3. **Умные рекомендации** - Автоматический поиск кандидатов на объединение

Приятной работы! 🚀
