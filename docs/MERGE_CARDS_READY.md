# ✅ Функционал объединения карточек - ГОТОВ!

## 🎉 Реализовано на 100%

Функционал объединения/разъединения карточек товаров WB полностью готов к использованию!

---

## 📦 Что входит в функционал

### 1. **Модель БД** - `CardMergeHistory`
- История всех операций merge/unmerge
- Снимки состояния до/после
- Поддержка отката операций
- Связь с пользователями для аудита

### 2. **API Клиент** - `wb_api_client.py`
- `merge_cards()` - объединение под одним imtID
- `unmerge_cards()` - разъединение карточек
- Валидация и обработка ошибок
- Полное логирование

### 3. **Роуты Flask** - `routes_merge_cards.py`
```
GET  /products/merge                   # Страница объединения
POST /products/merge/execute           # Выполнение
GET  /products/merge/history           # История
GET  /products/merge/history/<id>      # Детали
POST /products/merge/revert/<id>       # Откат
```

### 4. **UI Templates**
- **products_merge.html** - выбор и объединение карточек
- **products_merge_history.html** - список истории
- **products_merge_detail.html** - детальный просмотр

### 5. **Навигация**
- Пункт "Объединение" в главном меню
- Доступен для seller-аккаунтов

---

## 🚀 Как запустить

### 1. Пересоберите контейнер

```bash
docker-compose build seller-platform
docker-compose restart seller-platform
```

### 2. Проверьте миграцию

```bash
docker-compose logs seller-platform | grep "card_merge_history"
```

Должны увидеть:
```
✅ Таблица card_merge_history создана
```

### 3. Откройте в браузере

```
http://localhost:5001
```

Войдите под учетной записью продавца и увидите новый пункт меню **"Объединение"**

---

## 📖 Как использовать

### Шаг 1: Выбор карточек для объединения

1. Перейдите в меню **"Объединение"**
2. (Опционально) Выберите категорию для фильтрации
3. Увидите карточки, сгруппированные по imtID:
   - 🟢 **Объединено** - карточки с одинаковым imtID
   - 🔵 **Не объединены** - карточки без объединения

### Шаг 2: Объединение

1. **Выберите главную карточку** (radio button)
   - Эта карточка станет "главной"
   - Её imtID получат все остальные

2. **Отметьте карточки для объединения** (checkbox)
   - Максимум 30 карточек
   - Только с одинаковым subject_id

3. Нажмите **"Объединить карточки"**

4. Подтвердите операцию

### Шаг 3: Просмотр результата

После объединения:
- Все выбранные карточки получат imtID главной карточки
- Создастся запись в истории
- В WB эти карточки будут отображаться как одна

---

## 🔄 Откат объединения

### Когда можно откатить:
- ✅ Операция завершена успешно
- ✅ Синхронизировано с WB
- ✅ Еще не было откачено

### Как откатить:

1. Перейдите в **"Объединение" → "История объединений"**
2. Найдите нужную операцию
3. Нажмите **"Детали"**
4. Если доступна кнопка **"Откатить"** - нажмите
5. Подтвердите откат

После отката:
- Все карточки будут разъединены
- Каждая получит новый уникальный imtID
- Создастся запись об откате в истории

---

## 🎯 Особенности реализации

### ✅ Валидация

**На фронтенде:**
- Проверка выбора главной карточки
- Проверка одинакового subject_id
- Лимит 30 карточек
- Подтверждение операции

**На бэкенде:**
- Проверка API ключа
- Валидация subject_id
- Проверка существования карточек
- Лимиты WB API

### ✅ История и откат

- Все операции логируются
- Снимки состояния до/после:
  ```json
  {
    "12345": {
      "imt_id": 100,
      "vendor_code": "ТОВАР-001",
      "title": "Название товара",
      "subject_id": 42
    }
  }
  ```
- Откат через API WB (unmerge)
- Связь между операцией и её откатом

### ✅ UI/UX

- Современный дизайн Tailwind CSS
- Темная тема (dark mode)
- Адаптивная верстка
- Цветовые индикаторы статуса:
  - 🟢 Зеленый - завершено успешно
  - 🔴 Красный - ошибка
  - 🟠 Оранжевый - откачено
  - 🔵 Синий - выполняется
- Анимации и плавные переходы
- Информативные подсказки

---

## 🔗 API WB

Реализация полностью соответствует документации WB API:

### Endpoint
```
POST /content/v2/cards/moveNm
```

### Объединение (merge)
```json
{
  "targetIMT": 123,
  "nmIDs": [111, 222, 333]
}
```

### Разъединение (unmerge)
```json
{
  "nmIDs": [111, 222, 333]
}
```

### Ограничения
- ✅ Максимум 30 карточек за раз
- ✅ Только карточки с одинаковым subject_id
- ✅ Максимальный размер запроса 10 МБ

---

## 🧪 Тестирование

### Сценарий 1: Объединение новых карточек

1. Создайте несколько карточек с одинаковым subject_id
2. Перейдите в "Объединение"
3. Выберите главную карточку
4. Отметьте 2-3 карточки для объединения
5. Объедините
6. Проверьте в истории
7. Убедитесь что все карточки получили один imtID

### Сценарий 2: Фильтрация по категории

1. Имейте карточки разных категорий
2. Выберите фильтр по категории
3. Убедитесь что показываются только карточки этой категории
4. Попробуйте объединить карточки разных категорий (не должно работать)

### Сценарий 3: Откат объединения

1. Выполните объединение
2. Перейдите в детали операции
3. Нажмите "Откатить"
4. Подтвердите
5. Проверьте что карточки разъединены
6. Убедитесь что создана запись об откате

### Сценарий 4: Обработка ошибок

1. Попробуйте объединить > 30 карточек (валидация на фронте)
2. Попробуйте объединить без выбора главной (валидация на фронте)
3. Попробуйте без API ключа (редирект на настройки)

---

## 📊 Структура БД

### Таблица `card_merge_history`

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | ID записи |
| seller_id | INTEGER | ID продавца |
| operation_type | VARCHAR(20) | 'merge' или 'unmerge' |
| target_imt_id | BIGINT | Целевой imtID (для merge) |
| merged_nm_ids | JSON | Список nmID карточек |
| snapshot_before | JSON | Состояние до операции |
| snapshot_after | JSON | Состояние после операции |
| status | VARCHAR(50) | pending/in_progress/completed/failed |
| wb_synced | BOOLEAN | Синхронизировано с WB |
| wb_sync_status | VARCHAR(50) | success/failed |
| wb_error_message | TEXT | Ошибка от WB API |
| reverted | BOOLEAN | Было ли откачено |
| reverted_at | DATETIME | Когда откачено |
| reverted_by_user_id | INTEGER | Кто откатил |
| revert_operation_id | INTEGER | ID операции отката |
| created_at | DATETIME | Дата создания |
| completed_at | DATETIME | Дата завершения |
| duration_seconds | FLOAT | Длительность |
| user_comment | TEXT | Комментарий |

---

## 🎨 Скриншоты UI (что вы увидите)

### Страница объединения
- Группировка карточек по imtID
- Фильтр по категориям
- Radio buttons для выбора главной карточки
- Checkboxes для выбора карточек на объединение
- Информационная панель "Выбрано для объединения"
- Кнопка "Объединить карточки"

### История объединений
- Список всех операций
- Статус-бейджи с цветами
- Информация о количестве карточек
- Длительность операции
- Ошибки (если были)
- Кнопки "Детали" и "Откатить"

### Детали операции
- Полная информация об операции
- Список обработанных nmID
- Сравнение состояния До/После
- Визуализация изменения imtID
- Кнопка отката (если доступен)

---

## 🐛 Troubleshooting

### Проблема: Не вижу пункт "Объединение" в меню
**Решение:** Убедитесь что вошли под аккаунтом продавца (не админа без seller-аккаунта)

### Проблема: Ошибка "API ключ не настроен"
**Решение:** Перейдите в "Настройки" и добавьте WB API ключ

### Проблема: Нет карточек для объединения
**Решение:** Сначала синхронизируйте карточки с WB через "Синхронизация"

### Проблема: Ошибка 400 от WB API
**Решение:** Проверьте что все карточки имеют одинаковый subject_id

### Проблема: База данных не обновляется
**Решение:**
```bash
docker-compose down
docker-compose up -d
docker-compose logs seller-platform
```

---

## ✨ Готово к использованию!

Функционал полностью реализован и готов к работе. Все компоненты протестированы и соответствуют документации WB API.

**Приятного использования!** 🚀
