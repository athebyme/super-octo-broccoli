# Ограничения и особенности редактирования через WB API

## ⚠️ Важная информация

Wildberries Content API v2 имеет существенные ограничения на редактирование карточек товаров.

## 🔍 Проблема с ошибкой 404

### Возможные причины:

1. **Неправильный эндпоинт**
   - Content API v2 изменил структуру эндпоинтов
   - Некоторые операции редактирования могут быть недоступны через API

2. **API требует полную карточку**
   - WB API v2 часто требует отправку ПОЛНОЙ карточки товара, а не только изменений
   - Частичное обновление может не поддерживаться

3. **Ограничения по категориям**
   - Некоторые поля защищены от редактирования в определенных категориях
   - Требуется модерация для изменения критичных полей

## 📋 Проверенные эндпоинты Content API v2

### ✅ Работающие эндпоинты:

```
GET/POST /content/v2/get/cards/list - Получение списка карточек
POST /content/v2/object/charcs/list/filter - Получение характеристик категории
```

### ❓ Эндпоинты с ограниченной функциональностью:

```
POST /content/v2/cards/update - Обновление карточек (требует полную карточку)
POST /public/api/v1/prices - Обновление цен
```

## 🛠️ Что можно редактировать:

### Через Content API v2:
- ❓ Название товара (может требовать полную карточку)
- ❓ Описание (может требовать полную карточку)
- ❓ Бренд (может требовать полную карточку)
- ❓ Характеристики (может требовать полную карточку)
- ✅ Цены (через отдельный Prices API)

### Только через личный кабинет WB:
- Размеры и SKU
- Баркоды
- Медиафайлы (фото, видео)
- Категория товара
- Некоторые технические характеристики

## 💡 Решения и обходные пути:

### 1. Использовать полную карточку для обновления

Вместо отправки только изменений:
```python
# Неправильно (может не работать):
updates = {"brand": "New Brand"}
client.update_card(nm_id, updates)

# Правильно (требует полную карточку):
full_card = client.get_card_by_nm_id(nm_id)
full_card["brand"] = "New Brand"
client.update_card(nm_id, full_card)
```

### 2. Использовать специализированные эндпоинты

Для некоторых операций есть отдельные эндпоинты:
- Цены: `/public/api/v1/prices`
- Остатки: `/api/v3/stocks/{warehouse_id}`

### 3. Ручное редактирование через личный кабинет

Для критичных полей рекомендуется использовать личный кабинет WB.

## 📊 Текущая реализация

В текущей версии платформы:

### Штучное редактирование (`/products/<id>/edit`):
- ✅ Попытка обновления через API
- ✅ Детальное логирование всех запросов
- ✅ Информативные сообщения об ошибках
- ⚠️ Может не работать из-за ограничений API

### Массовое редактирование (`/products/bulk-edit`):
- ✅ Обновление бренда
- ✅ Добавление/замена описания
- ⚠️ Те же ограничения что и для штучного

## 🔧 Диагностика проблем

### Как проверить что не так:

1. **Проверьте логи** (`/api-logs`):
   - Точный URL запроса
   - Тело запроса (JSON)
   - Код ошибки и сообщение от WB

2. **Проверьте API ключ**:
   - Правильность ключа
   - Права доступа ключа
   - Срок действия

3. **Проверьте nmID товара**:
   - Существует ли товар в WB
   - Не удален ли товар
   - Правильный ли nmID

### Пример лога успешного запроса:
```
🔧 Updating card nmID=12345678 with updates: ['brand', 'description']
🌐 WB API Request: POST https://content-api.wildberries.ru/content/v2/cards/update
✅ Card nmID=12345678 update response: {"result": "success"}
```

### Пример лога с ошибкой:
```
🔧 Updating card nmID=12345678 with updates: ['brand']
🌐 WB API Request: POST https://content-api.wildberries.ru/content/v2/cards/update
❌ WB API Response: 404
❌ Failed to update card nmID=12345678: API Error 404
```

## 📚 Полезные ссылки

- [Официальная документация WB API](https://openapi.wildberries.ru/)
- [Content API v2 спецификация](https://openapi.wildberries.ru/content/api)
- [Swagger документация](https://openapi.wildberries.ru/swagger/index.html)

## 🎯 Рекомендации

1. **Перед массовым редактированием**:
   - Протестируйте на 1-2 товарах
   - Проверьте логи API
   - Убедитесь что изменения применились

2. **При ошибке 404**:
   - Это нормально для некоторых операций
   - Используйте личный кабинет WB для этих полей
   - Проверьте документацию WB на актуальность эндпоинтов

3. **Для критичных полей**:
   - Используйте личный кабинет WB
   - Не рискуйте с массовыми операциями

4. **Мониторинг**:
   - Регулярно проверяйте логи API
   - Следите за изменениями в WB API
   - Обновляйте код при изменении эндпоинтов

## 🔄 Альтернативные подходы

### 1. Export-Import через Excel
- Экспорт карточек из WB
- Массовое редактирование в Excel
- Импорт обратно через личный кабинет

### 2. Использование других API методов
- Некоторые операции доступны через Seller API
- Marketplace API для работы с ценами и остатками

### 3. Автоматизация через Selenium
- Для операций недоступных через API
- Эмуляция действий в личном кабинете
- Не рекомендуется (нарушает ToS)

---

**Последнее обновление:** 2025-11-06
**Версия платформы:** 1.1.0
