# WB Seller Platform - Платформа для продавцов Wildberries

Платформа для работы с отчетами Wildberries с авторизацией и управлением продавцами.

## Технологии

### Backend
- **Flask** - веб-фреймворк
- **Flask-Login** - авторизация пользователей
- **Flask-SQLAlchemy** - ORM для работы с БД
- **SQLite** - база данных
- **Werkzeug** - хеширование паролей

### Frontend
- **TailwindCSS** - современный CSS фреймворк
- **Alpine.js** - реактивные компоненты
- **Чистый HTML** - легко редактировать и расширять

## Возможности

### Для администраторов
- ✓ Добавление/редактирование/удаление продавцов
- ✓ Управление доступом (активация/деактивация)
- ✓ Просмотр статистики по продавцам
- ✓ Заметки к каждому продавцу

### Для продавцов
- ✓ Личный кабинет
- ✓ Дашборд с информацией о компании
- 🔜 Просмотр отчетов и аналитики
- 🔜 Работа с накладными
- 🔜 Анализ прибыли

### Безопасность
- ✓ Хеширование паролей (Werkzeug)
- ✓ Защита сессий
- ✓ Разделение прав (админ/продавец)
- ✓ CSRF защита (встроена в Flask)

## Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Инициализация платформы

Запустите скрипт инициализации для создания БД и первого администратора:

```bash
python init_platform.py
```

Скрипт запросит:
- Имя пользователя администратора
- Email
- Пароль (минимум 6 символов)

### 3. Запуск приложения

```bash
python seller_platform.py
```

Или используя Flask CLI:

```bash
flask --app seller_platform run
```

Приложение будет доступно по адресу: `http://localhost:5001`

## Структура проекта

```
.
├── seller_platform.py      # Основное приложение
├── models.py               # Модели БД (User, Seller)
├── init_platform.py        # Скрипт инициализации
├── templates/              # HTML шаблоны
│   ├── base.html          # Базовый шаблон
│   ├── login.html         # Страница входа
│   ├── dashboard.html     # Дашборд продавца
│   ├── admin.html         # Админ панель
│   ├── add_seller.html    # Добавление продавца
│   └── edit_seller.html   # Редактирование продавца
└── seller_platform.db      # База данных SQLite (создается автоматически)
```

## Использование

### Вход в систему

1. Откройте `http://localhost:5001/login`
2. Введите учетные данные
3. После входа вы попадете на дашборд

### Добавление продавца (для админа)

1. Перейдите в "Админ панель"
2. Нажмите "Добавить продавца"
3. Заполните форму:
   - **Обязательные поля**: имя пользователя, email, пароль, название компании
   - **Опциональные**: телефон, WB ID, заметки
4. Нажмите "Создать продавца"

### Редактирование продавца

1. В админ панели нажмите "Редактировать" у нужного продавца
2. Измените нужные поля
3. Можно изменить пароль (оставьте пустым, чтобы не менять)
4. Можно активировать/деактивировать аккаунт

### Удаление продавца

1. В админ панели нажмите "Удалить" у нужного продавца
2. Подтвердите действие

## API маршруты

### Авторизация
- `GET/POST /login` - Вход в систему
- `GET /logout` - Выход из системы

### Дашборд
- `GET /` или `GET /dashboard` - Главная страница (требуется авторизация)

### Админ панель (требуются права администратора)
- `GET /admin` - Список продавцов
- `GET/POST /admin/sellers/add` - Добавление продавца
- `GET/POST /admin/sellers/<id>/edit` - Редактирование продавца
- `POST /admin/sellers/<id>/delete` - Удаление продавца

## Модели данных

### User (Пользователь)
- `id` - ID пользователя
- `username` - Имя пользователя (уникальное)
- `email` - Email (уникальный)
- `password_hash` - Хеш пароля
- `is_admin` - Флаг администратора
- `is_active` - Активен ли аккаунт
- `created_at` - Дата создания
- `last_login` - Последний вход

### Seller (Продавец)
- `id` - ID продавца
- `user_id` - Связь с пользователем
- `company_name` - Название компании
- `wb_api_key` - API ключ WB (опционально)
- `wb_seller_id` - ID продавца в WB
- `contact_phone` - Контактный телефон
- `notes` - Заметки администратора
- `created_at` - Дата создания
- `updated_at` - Дата обновления

## Дальнейшее развитие

Следующие шаги для расширения платформы:

1. **Интеграция с WB API**
   - Автоматическая загрузка отчетов
   - Получение накладных
   - Синхронизация товаров

2. **Анализ прибыли**
   - Расчет с правильными ценами из накладных
   - Графики и визуализация
   - Экспорт отчетов

3. **Работа с документами**
   - Загрузка и хранение накладных
   - Парсинг цен из накладных
   - История изменений

4. **Уведомления**
   - Email уведомления
   - Уведомления о новых отчетах
   - Напоминания

## Разработка

### Добавление новых страниц

1. Создайте маршрут в `seller_platform.py`:
```python
@app.route('/new-page')
@login_required
def new_page():
    return render_template('new_page.html')
```

2. Создайте шаблон `templates/new_page.html`:
```html
{% extends "base.html" %}
{% block content %}
<!-- Ваш контент -->
{% endblock %}
```

### Изменение стилей

Стили используют TailwindCSS через CDN. Для изменения дизайна просто редактируйте классы в HTML шаблонах.

### Расширение моделей

Добавьте поля в `models.py` и выполните миграцию:

```python
# В models.py добавьте новое поле
new_field = db.Column(db.String(100))

# Затем пересоздайте БД или используйте Alembic для миграций
```

## Поддержка

При возникновении проблем проверьте:
1. Установлены ли все зависимости из `requirements.txt`
2. Инициализирована ли база данных (запустите `init_platform.py`)
3. Правильные ли учетные данные при входе

## Лицензия

Проект создан для внутреннего использования.
